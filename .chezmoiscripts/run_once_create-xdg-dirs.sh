#!/bin/bash
# Create XDG directories (runs once per machine)

mkdir -p "$HOME/.config"
mkdir -p "$HOME/.cache"
mkdir -p "$HOME/.local/share"
mkdir -p "$HOME/.local/state"
mkdir -p "$HOME/.cache/fish"
mkdir -p "$HOME/.local/state/less"
mkdir -p "$HOME/.local/share/gem"
mkdir -p "$HOME/.config/bundle"
mkdir -p "$HOME/.ssh/sockets"
