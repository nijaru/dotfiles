# Supply Chain Security: 3-day age gate to mitigate zero-day malware.
exclude-newer = "3 days"

# Per-package overrides: these bypass the age gate completely
exclude-newer-package = { uv = false, ruff = false, ty = false }
